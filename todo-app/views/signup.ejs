<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title> <%= title %></title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header') -%>
            <h6 class="py-4">SignUp as new User</h6>
            <form action="/users" method="post" >
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="py-3">
                    <label>First Name</label>
                    <input type="text" name="firstName" class="border w-full rounded">
                </div>
                <div class="py-3">
                <label>Last Name</label>
                <input type="text" name="lastName" class="border w-full rounded">
                </div>
                <div class="py-3">
                <label>Email</label>
                <input type="email" name="email" class="border w-full rounded">
                </div>
                <div class="py-3">
                <label>Password</label>
                <input type="password" name="password" class="border w-full rounded">
                </div>
                <button type="submit" class="bg-green-500 py-2 px-2 border rounded text-white">SignUp</button>

            </form>
        </div>
    </div>
    
</body>
</html>